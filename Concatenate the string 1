#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    char str1[100], str2[100], str3[200];
    scanf("%s", str1);
    scanf("%s", str2);
    strcpy(str3, str1);
    strcat(str3, str2);
    printf("%s\n", str3);
    return 0;
}
